@page "/texte/sammlungen/{id:guid}"
@model KeyWars.Pages.Texte.Sammlungen.DetailsModel
@{
    ViewData["Title"] = Model.Collection.Name;
}

<section class="page-header">
    <div>
        <h1>@Model.Collection.Name</h1>
        <p class="lead">@Model.Collection.Description</p>
    </div>
</section>

<section class="grid text-library-grid collection-text-grid">
@foreach (var text in Model.Texts)
{
    <article class="card text-card collection-text-card">
        <div>
            <h2>@text.Title</h2>
            <div class="metadata-row">
                <span class="meta-badge">@text.CharacterCount Zeichen</span>
                <span class="meta-badge">@(text.IsStandard ? "Standard" : DisplayNames.For(text.Visibility))</span>
            </div>
        </div>
        <div class="card-actions">
            <a class="button" asp-page="/Spielen/Text" asp-route-id="@text.Id">Trainieren</a>
            <a class="button secondary" asp-page="/Texte/Details" asp-route-id="@text.Id">Ansehen</a>
        </div>
    </article>
}
</section>
